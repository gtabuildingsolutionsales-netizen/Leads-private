# Anthropic
ANTHROPIC_API_KEY=

# Perplexity
PERPLEXITY_API_KEY=

# Supabase (use the service-role key — server-side only, never ship to a client)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Email (Gmail App Password works; defaults assume Gmail)
EMAIL_USER=
EMAIL_PASS=
FROM_NAME=Triple M Lending
FROM_EMAIL=
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Optional — qualified-lead alerts
SLACK_WEBHOOK_URL=
